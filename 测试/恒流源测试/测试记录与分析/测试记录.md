# 测试记录

## 第一次测试（5.14周四晚）

第二版需要修改：
元件临时替换
C29（1uF）（忘买了）暂时用10uF替代✔
C24（22uF/63V）（封装和耐压不匹配）暂时用10uF/50V替代✔
R15和R21（以及R8、R37）100kΩ（忘买了）暂用插件替代✔
SMD焊盘不匹配：U4、U8、U13、U14 ✔
部分丝印被遮挡/未标出，R22与R23的丝印反了（还有哪反了？）✔
LED1坏了✔
测试结论：除了可调buck以外的供电/降压电路都没有问题

## 第二次测试（5.16周六早）

万用表测不了贴片电阻阻值（R22=10kΩ较精确）
因为R15断开导致5V降不了，D4烧毁
由实测数据拟合得到可调buck输出公式：VOUT+ = 31.242 - 10.011 * V_DAC1
由公式计算最值：0（-1.794）~31.242V 范围合适
有误差（比如4.8/5）但没什么问题。（因为公式精度只影响 Buck 输出和 MOS 余量估计，误差 0.2V 左右可以接受，而且最后可以靠adc反馈自动微调）
负载45Ω，VOUT+二十几伏/15V，给SWIN使能，
DAC_CH2给0.1V，测得mos栅极1.55V，漏极23.694V，源极约为0
DAC_CH2给0.2V，栅极1.6V，漏极接近VOUT+，源极仍为0
记录了调整DAC_CH1时VOUT+的响应速度，VOUT+向下调很快，向上则较慢（后续调整哪里的电容？）
测试结论：可调buck降压电路没问题，mos和运放还需研究
需要进一步测试：
DAC_CH2给电压时，运放IN+（U2pin3）是否有电压？
运放的各引脚电压（主要是IN+-、OUT）看运放是否正常工作

## 第三次测试（5.17周日早）

过程记录：
R3附近丝印VOUT+标错了吧，那个应该是ADCREFGND，所以0V是正常的

---

VIN为15V，负载45Ω，DACCH1给0，DACCH2给0，
VOUT+为14.897，VOUT-为14.839
VOUT+为14.890，VOUT-为14.824
VOUT+为14.894，VOUT-为14.817
上电时间在5分钟以内
观察到，单片机绿色led自动熄灭——应该是自动复位
继续测，5V、3.3V、bucken、swin都正常，估计是噪声使mcu复位
Mos数据：
Vg=1.557V，Vd=VOUT-=14.846V，Vs=0
观察到IN+没法接受到DACCH2的0.2V电压，怀疑断路，发现R1确实没焊好，于是重新焊好R1

---

DACCH2不给电压：
Vload=1.871
VOUT+=14.574
VOUT-=12.750

---

Vload=0.350
VOUT+=14.877
VOUT-=14.526
Mos：
Vg=1.7V
Vd=VOUT-
Vs=17mV

---

VOUT+=14.520
VOUT-=10.356
Vload=4.149
电源显示电流0.10~0.11A
Vd=2V，Vs=195mV
Mos很烫（因为15V对mos来说太多）

---

VIN改成8V后，发现不使能时，VOUT+还有一点多伏，接在万用表两端正在稳定缓慢放电。我每次等到放电放至10mV以内时再使能，发现VIN无论是8V还是15V，使能后都没有再出现电流很大的现象——我猜和哪个电容没放完电有关。

---

VIN给8V，确保VOUT+放电完毕，还没开始使能，发现单片机红色电源灯自己熄灭了，但重新上电还能亮，我就暂时没管这个继续测了
打算绕过R1，0.2V不给DACCH2直接给com脚：
模拟开关Com给0.2V，电源显示电流到0.12A，
此时VOUT+=7.7V，VOUT-=3V，Vload=4V
且马上撤去com电压，只要上电&使能，0.12A（0.11A）就能保持——这个肯定是放电问题，但又不是VOUT+的放电问题，因为VOUT+我再看一次确保使能前是0V了，但再使能还是有0.11A的电流
我再次测试，使能后还是0.11A，测了com的电压，从约200mV马上减小到约0V，同时观察到0.11A变成0.02A（多次复现后确认是com脚的放电问题，而且要使能后放电效果最好）
过了十分钟吧，再使能，电流掉到0.04A了（说明com脚自动放电很慢）
没使能的时候测com，从24mV一下放电为0V，这时再使能就是0.02A了
单片机电源灯自动熄灭原因分析：我让com悬浮后又放电后，接着马上重新上电的一小段时间内，单片机电源灯不亮，我测5V和3V3都是正常的，然后测完就发现单片机又能一直稳定亮了——单片机的供电也受到了放电的影响？（但这是在放电后的影响）总之这个隔一小段时间才能稳定给单片机供电的现象是可以复现的。
测模拟开关：
SWIN=0，u1的pin2/3有一个是-35mV，其他都正常
SWIN=1，没有不符合预期的异常数据。Agnd也都是精确的0V。
数据分析：
0.01A/15V和0.02A/8V是数字电源待机输入电流，表示基本无负载输出；
0.12A是电源输入电流，适合仅作为有无负载输出的状态判断，不用计算
标红数据：Is= Vload / 45Ω ≈0.089A，与理论值0.1A目标电流接近（先不纠结误差，因为实验数据记录并不严谨和精确）
测试结论：
运放和mos都是正常的，模拟开关本身应该也没问题，问题定位到模拟开关的com脚上
猜测原因：
撤掉手动电压后是开路，不是 0V；COM/+IN 有 C1 和高阻输入，所以会存电。（“断开0.2V”和“把该电源通道输出设成0V并保持开启”是不同的，dac输出0V也不是悬空，而是把输出拉到0V附近）
如果是这样，那么保证单片机和dac芯片的稳定通信就能解决问题，但还是建议焊上下拉电阻47kΩ（并在第二版中做相应修改）

## 第四次测试（5.21周四晚）

可调buck分压问题：DAC_CH1为0.75时，VOUT+预期是23.73V，以后记得VIN要调到大于VOUT+预期值
DAC_CH1不受单片机指令控制，一直保持=FB电压（0.75V），说明DAC_CH1输出是高阻态（被FB拉到相同电压），说明DAC芯片没有输出
DAC芯片引脚电压记录
pin1：DAC_CH1 0.75
pin2：DAC_CH2 0
pin4：LDAC# 0
pin5：CLR# 3.3V
pin6：SYNC# 3.3V
pin7：SCLK 0
pin8：DIN 0
pin9：AVDD 3.3V
pin10：VREFIN/VREFOUT 0
示波器可以看到spi的方波信号（到底是哪个脚的信号？）
要做dac单独模块排查
BUCK_EN = 0
只保留 3.3V
直接测 U3 pin1 / pin2 / pin10
先让 DAC 本体输出 0V、1V、2V 成功
再接回 Buck 和恒流环
串口输出、adc、编码器✅
除了dac模块其他都没问题，先跳过dac，继续手动给dac1和2的电
尝试复现测试三现象，但给了一次dacch2的0.2V后，单片机就自动复位了（绿灯灭），撤去0.2V，一按下使能按键单片机就复位，绿灯按下去才亮松开就灭（使能了才复位？还是单片机一直在复位？）用万用表给com放电后单片机才可以稳定开启使能
在COM右端和agnd测试点焊盘之间焊上47kΩ后：
为什么一给com脚0.2V，单片机同时自动复位？并且用万用表测com脚两端电压，发现电压从0V附近稳定上升到70mV，有时又跳变到更低，但最终这么做时间长了（至少半分钟）都会变成突然跳到5V输出（不是手不稳定的问题，附近也没有5V的电压）同时伴随着单片机电源灯熄灭？（此现象发生过至少三次，且最终都没有损坏单片机）
47kΩ电阻真的形成放电路径了吗？有时测com脚电压，能逐渐上升到十几二十几甚至更高mV最后突变到5V，有一两次又稳定在一点多/二点多mV上。而万用表的测量其实也是在放电，这样就不确定电是谁放的、万用表又引入了什么影响——AI建议还是要断电测电阻
AI建议测试操作2：

1. 全部断电，VOUT+ 放干净。
2. 测 COM、AGND、U1 pin4、U2 pin3、U2 pin6、+5V 之间的电阻。
3. 如果 COM 到 U2 OUT 或 +5V 不是开路/高阻，先清焊点、洗助焊剂、重焊 47k。
4. 上电但 BUCK_EN=0，不给 COM 0.2V，只测 COM、U2 +IN、U2 OUT 是否自己爬升。
   （此现象没尝试复现过）运放的out脚，在com电压跳变到5V后，测得接近5V。但后面com正常时out脚就是1.2（还是1.6来着）左右
   对比第三次测试，既然接线和硬件相同，为什么那次就没有“一给”0.2V就复位的问题？是因为（之前代码只用到了bucken和swin的同时使能按键）新的代码复杂度增加影响了单片机吗？还是今天连线哪里有问题？（拆下来接上去过好几次，大概率没问题）
   Safe_off能直接产生类似复位的现象，可能导致混淆，这个很可疑
   ——先禁用safeoff的灭灯功能，看看下次测试还有没有绿灯熄灭现象
   Dacch2是否能代替com，这也还没验证（模拟开关）
   5V到底是哪来的？这是最奇怪的问题（其实我只有第一次观察到了5V，后面都是听到万用表切换到V档、看见单片机电源灯熄灭，而并没有直接看到是5V还是几V）
   观察到用手碰焊上去的47kΩ下拉电阻时，电源电流从0.02变成0.03/0.04A

下一步测试：
Dac芯片独立测试
5V来源硬件检查（见“测试操作2”）
Safeoff代码清零验证

## 第五次测试（5.22周五晚）

Dac芯片独立测试：
烧录了ai提供的第一版代码，只给3V3焊盘上电，前四个（见聊天记录）都符合预期，就是pin10没有输出（测的全都是电容不是小引脚）
做了几轮代码（包括软件spi）和硬件检查，结论是要么是焊接导致芯片整体偏移，要么是芯片本身真的坏了，最好重新焊接一下，还不行就是芯片坏了，干脆换一个芯片
5V来源硬件检查：
【给下一块板的启示】
DAC 输出到运放 +IN 不要经过容易悬浮的模拟开关，直接到运放输入，中间可串 100Ω～1k 保护电阻；
运放 +IN 必须有明确下拉/默认 0V 路径；
DAC 输出、运放 +IN、运放 OUT、MOS Gate、+5V 都留测试点；
高阻模拟节点周围少放脏焊点、飞线、临时插件；
不要再让“断开输入”变成悬空状态。
新图建议这样画：
DAC_OUT ── 100Ω~1k ── 运放 +IN
│
47k~100k
│
AGND
没来得及做VIN上电时的检查
Safeoff代码清零验证：
没来得及验证去掉safeoff后，是否还有“com给电后一按按键绿灯就灭”的现象
下一步（最后）测试：
Dac芯片重焊，烧ai给的测试代码，看能不能开启2.5V参考电压，不行就放弃这个芯片

## 第六次测试（5.23周六早）

Dac8562还是不行，遂放弃，画新的原理图并重新打板

## 第七次测试（5.28周日晚）

新板子dac芯片本身正常了，出现了一些代码上的问题

## 第八次测试（5.30周六早）

硬件部分大致测试通过，剩下软件

## 第九次测试（5.31周日晚）

VOUT+消失——发现是因为R21断路
dac1的设定值与实际值✅
dac1与Vout+的公式get✅
Vsense的采样值（算得实际电流）与用电流表测得实际电流的线性校准公式get✅（还没算好）
一个注意事项：电源限流要设到0.5A，否则CV切换到CC使3V3和5V掉电波动、单片机复位
发现电位器在450mA（45欧）的时候就会烧（所以这次没测500mA，要测的话就之后调小电阻吧）
获取测试数据01

## 第十次测试（6.2周二晚）

获取测试数据02，算出校准公式

## 第十一次测试（6.4周四晚）

校准公式不准
代码重构

## 第十二次测试（6.6周六早）

代码重构完成，剩下小电流Idisp校准、显示滤波问题、三大保护逻辑、…

## 第十三次测试（6.9周二晚）

电流校准完成，pi与滤波调参完成

## 第十四次测试（6.11周四晚）
VOUT+-与Rload校准完成
BUCK慢环添加完成
电流重新校准完成
三大保护测试通过
外壳设计完成