此目录中是项目代码的基本架构，使用HAL库开发，其中每个文件夹中都有**Inc**和**Src**，分别存放头文件和主文件。

各文件夹功能：
- Lib用于存放库文件
- App用于存放应用文件
- Core中暂时只放main文件
- 若要修改其它文件，可将相应文件和所在文件夹添加进来

使用这种开发模式的原因是不同开发者的编程环境和编译链不同，不能直接放上整个项目，所以按照模块放上需要修改的部分即可。