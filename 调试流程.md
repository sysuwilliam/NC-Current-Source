# 调试流程

目前已经确认：除了可调 buck 以外的供电/降压电路都没有问题。

## 一、测试规范

- **使能 `BUCKEN` 前，要先确保 `DAC_CH1` 不是 `0 V`。**  
  因为 `0 V` 对应 `VOUT+` 最大输出目标。若此时负载较小，MOS 管发热严重，可能会烧毁。

## 二、测试流程

### 1. 测量分压电阻并粗略计算 VOUT 公式

测量 `R20`、`R21`、`R22` 的实际阻值，粗略计算公式：

```text
Vout = A - B * Vdac
```

得到参数 `A`、`B`，判断 `VOUT+` 的临界值情况。

#### 1.1 最大值

- 如果低于 `28 V` 左右，就必须调整电阻。  
  `28 V` 是极限，余量偏紧。
- 如果约 `29 V`，可以接受。
- 如果能到 `30 V`，最好。

#### 1.2 最小值

- 如果约 `3~4 V`，很理想。
- 如果约 `5 V`，可以接受。
- 如果大于 `5 V`，得改电阻。

> 不改电阻只是不能测边界情况，不影响接下来的测试。  
> 所以考虑到时间原因，如果有这个问题可以先搁置。

### 2. 低压测试

如果 `VOUT` 范围合适，就进行低压测试，例如：

```text
VIN = 12 V
限流 = 0.5 A
```

#### 2.1 验证 DAC_CH1 与 VOUT+ 的趋势

- 接负载：`50 Ω`
- 目标电流：设为 `0`（`DAC_CH2`）
- 单片机使能 `BUCKEN`
- 可能需要测一下是否成功给出 `3.3 V`
- 测量 `Vdac` 和 `VOUT+`
- 验证趋势：`DAC_CH1` 越高，`VOUT+` 是否越低

#### 2.2 验证低输出范围下的恒流效果

- 目标电流改为：`50 mA`
- 在 `5~8 V` 范围内改变 `VOUT+`（通过 `DAC_CH1`）
- 观察电流是否基本稳定在 `50 mA`
- 验证低输出范围是否正常

### 3. MOS 裕量不足测试

目的：

- 熟悉 MOS 裕量不足时的现象。
- 找出大概可参考的 `Vds` 最小边界。

注意：这个测试**代替不了** `500 mA & 50 Ω` 条件下的最小边界测试，后续还得另测。

#### 3.1 测试条件与步骤

- 负载：`50 Ω`
- 目标电流：`50 mA`
- 先让 `VOUT+` 保持足够余量，例如 `6~8 V`
- 再逐步降低 `VOUT+`
- 记录 MOS 裕量不足时的临界 `Vmos`

```text
Vmos = VOUT- - Vsense
```

#### 3.2 观察 MOS 裕量不足的现象

- `Vgate` 会升高，甚至接近运放输出上限。
- 实际电流达不到设定值。
- `Vsense` 开始低于 `DAC_CH2`。

### 4. 高压测试

测量 `3~5` 组数据，例如：

```text
5 V、10 V、20 V、28 V、30 V
```

通过拟合进一步确认参数 `A`、`B`，得到更精确的公式。

原因：实际电路中还要考虑：

- 电阻误差
- DAC 输出误差
- LMR16030 误差
- FB 基准误差
- PCB 实际情况

所以，只测三个电阻得到的公式精度不够。

### 5. 后续还可以测什么

- `100 mA` 恒流测试
- 不同负载下的低电流测试：`10 Ω / 25 Ω / 50 Ω`
- 短路低电流限流测试，例如 `50 mA`
- 其他边界条件测试

## 三、备注

### 1. VDAC_CH2 与目标电流的关系

```text
Vdac2 = Vsense = I * 2 Ω
```

### 2. 自动测试阶段的软件限制

暂时不用，但先记录如下。

之后上自动测试时，要在软件里限制：

```c
VDAC_MIN = 对应 VOUT = 30 V 的 DAC 值;
VDAC_MAX = 对应 VOUT = 5 V 的 DAC 值;
```

> 这个手动阶段应该还用不上。
