# 总体说明
- 电源树
  ```
   35V 输入
    ├── 可调 Buck：V+，给负载 + MOS 恒流功率级，5~30V
    └── 固定 Buck：5V
        ├── 串口屏
        ├── OPA197 运放
        └── LDO 3.3V
              ├── STM32
              ├── DAC
              ├── TS5A3159A
              └── 比较器/ADC参考相关小信号电路
  ```
- 恒流控制逻辑
  ```
  硬件恒流环：运放 + MOS：毫秒甚至更快，负责稳电流
  buck余量控制环：MCU + Buck：几十毫秒到几百毫秒更新一次，负责控发热
  【后期优化再加】电流校准环：MCU 读取 Iactual，发现 Iactual 和 Iset 有小偏差 -> 很慢很慢地微调 DAC_CH2
  ```
  流程：
  ```
  用户设定 Iset
      ↓
  MCU 输出 DAC_CH2 = Iset × Rs
      ↓
  运放 + MOS + Rs 硬件恒流
      ↓
  MCU 读取 Vsense，显示 Iactual，做保护判断
      ↓
  MCU 读取 VOUT+、VOUT-、Vsense
      ↓
  计算 VMOS = VOUT- - Vsense
      ↓
  慢速调整 DAC_CH1，改变 Buck 输出 V+
  ```
---
# 计算与采样
- 采样点
  - PA0 = Vsense，采样电阻电压，算电流
  - PA1 = OUT+ / V+，Buck 输出，负载上端
  - PB0 = OUT- / MOS Drain，负载下端
  

- 输出电流计算
  ```
  DAC_ISET：0~1.0V
  Iout = DAC_ISET / 2Ω
  ```
- DAC->可调buck计算
  ```
  V_OUT ≈ 30.98 - 10 * V_DAC
  DAC_CH1 工作范围大约：0.1V ~ 2.6V (对应 30V ~ 5V)
  ```
- 负载电阻计算
  ```
  VOUT+ = ADC_VOUT+ × 11 × 校准系数
  VOUT- = ADC_VOUT- x 11 × 校准系数
  Vload = VOUT+ - VOUT-
  Rload = Vload / Iout
  ```
- mos压降计算
  ```
  VMOS​=VOUT-​ - Vsense​
  ```
---
# 功能实现
- 过流保护实现
  - 流程：
    ```
    Vsense → MCU ADC

    MCU_GPIO → 模拟开关IN & 可调 Buck EN

    MCU → 串口屏
    ```
  - 示例代码：
    ```
    if (Vsense > 1.10V) {
      SW_IN = 0;          // 模拟开关切到 GND，Iset=0
      DAC_ISET = 0;       // DAC 输出也归零
      BUCK_EN = 0;        // 关闭可调 Buck
      fault_oc = 1;       // 锁存过流故障
      show_alarm();       // LCD 显示过流
    }
    // 注意这里的 alarm_flag 要锁存，不要电流一降下来就自动清除；可以直到用户按复位按键（不是单片机上的）再清...
    ```
- 短路保护实现
  - 判定
    ```
    set > 20mA
    Iactual ≈ Iset
    Vload < 0.2V ~ 0.5V
    持续 50ms~200ms
    (电流能达到设定值，但负载电压接近 0)
    ```
  - 执行
    ```
    把 Buck 输出 V+ 降到较低值  (V+​ ≈ Iset * ​Rs ​+ VMOS余量​)
    LCD 显示“短路限流”
    ```
  - 解除
    ```
    短路解除后，负载电压不再接近 0，程序重新进入正常 Buck 调节逻辑，慢慢把 V+ 升到需要的值
    这个保护不需要锁存，可以自动恢复
    ```
- 开路保护
  - 判定
    ```
    Iset > 20mA
    Iactual < 5mA
    Buck 已经升到较高值 / 或 V+ 接近当前目标值
    持续 200ms
    => 开路
    ```
  - 执行
    ```
    1. 模拟开关 IN 拉低 → Iset 接 GND
    2. DAC_ISET = 0
    3. 可调 Buck EN 关闭，或者 V+ 降到最低
    4. LCD 显示“负载开路”
    5. 等待用户接好负载后手动清除报警
    ```
  - 解除
    ```
    按ALARM CLEAR按键 （能混用吗？）
    ```

---
# 交互内容
- 外设
  ```
  1 个 带按压旋转编码器
  1 个 OUTPUT / STOP 按键
  1 个 ALARM CLEAR 按键
  1 个 备用按键
  ```
- 交互实现
  - 设置目标电流：旋转编码器
  - 开启输出：按OUTPUT
  - 触发过流保护/开路保护后的恢复：按ALARM CLEAR
  - ALARM CLEAR 状态机
    ```
    正常输出状态
        ↓
    检测到过流 / 开路
        ↓
    FAULT 状态：
        SW_IN = 0
        DAC_ISET = 0
        BUCK_EN = 0
        显示报警
        ↓
    用户按 ALARM_CLEAR
        ↓
    IDLE 待机状态：
        报警清除
        输出仍关闭
        ↓
    用户再按 OUTPUT/STOP
        ↓
    重新启动输出
    ```
---
# 单片机引脚分配

| 功能                  | 建议引脚                  | 说明                      |
| ------------------- | --------------------- | ----------------------- |
| 调试串口                | USART1：PA9 TX，PA10 RX | 接 USB-TTL               |
| 串口屏                 | USART2：PA2 TX，PA3 RX  | 接屏幕                     |
| DAC SPI             | SPI1：PA5 SCK，PA7 MOSI | DAC8562 不需要 MISO        |
| DAC SYNC            | PA4                   | 普通 GPIO                 |
| OUT- / MOS Drain 采样         | PA0 / ADC_IN0         | 最重要                     |
| OUT+ / Buck V+ 采样   | PA1 / ADC_IN1         | 高压分压                    |
| 电流采样 Vsense | PB0 / ADC_IN8         | 高压分压                    |
| 编码器 A/B             | PB6 / PB7         | 推荐用 TIM4_CH1 / TIM4_CH2 |
| 编码器按下 SW            | PB8                   | 普通 GPIO，输入上拉            |
| OUTPUT / STOP       | PB12                  | 普通 GPIO，输入上拉            |
| ALARM CLEAR         | PB13                  | 普通 GPIO，输入上拉            |
| 备用按键                | PB14                  | 普通 GPIO，输入上拉            |
| 模拟开关 IN             | PB10                  | 输出，高=接 DAC，低=接 GND      |
| Buck EN             | PB11                  | 输出，使能/故障关断              |
| SWD                 | PA13 / PA14           | 保留下载调试                  |



---
# 原理图元件说明
### R5（运放IN-与Rs上端之间）（对应开源工程中R17）
- 取47Ω（或0Ω/100Ω）不影响直流精度，因为运放输入电流非常小，R5上几乎没有压降
- 用处：
  - 调试时方便断开：万一反馈接错、噪声很大，拆掉 R17 就能把运放反相端和采样点隔离开，方便定位问题
  - 隔离采样电阻大电流节点上的尖峰噪声

### R4（R_GATE）（对应开源工程中R16）
- 默认 47Ω ，如果响应太慢可以改 22Ω；如果振荡可以改 100Ω

### R1(COM到IN+)（对应开源工程中R15）
- DAC 到同相端最好还是留一个串联电阻，用来隔离 DAC、模拟开关和输入滤波

### R2（采样电阻）
- 2Ω 0.1% 
- ≤25ppm/℃
- ≥2W，最好 3W
- 2512 或四端 Kelvin 采样电阻

### C2（运放IN-到OUT）（对应开源工程中C23）
- 取值：不焊 / 330pF / 1nF 
- 开源工程原来用 C23=10nF，是配合 LMH6642、R15=2k、R17=1k、R19=2.49Ω 那套反相求和结构的，现在结构已经变了，不能照抄。调试时先不焊 C23；如果示波器看到 Vsense 有持续高频振荡，再往空焊盘上加 330pF。没有振荡就一直不焊。若加了还振，换 1nF；如果响应太慢，再拆掉。

### C6/7/8（ADC跨板滤波）
- 默认不焊，若ADC抖动较大再焊1nF/10nF

### C27/C28（VOUT+-采样）
- C越小响应越快，越大滤波效果越好
- 先取22nF，如果buck开关噪声明显、显示抖，再改大到 100nF
- PCB布局注意：
  ```
  R25 和 C27 尽量靠近 MCU ADC 引脚
  R19/R24 靠近被采样的 VOUT+ 节点
  （VOUT-采样电路同理）
  ```

### C35（V_SENSE滤波）
- 暂用 10nF，如果 ADC 抖得厉害，可以把 C35 改到 22nF 或 100nF


### mos管
- 封装：TO220是最好散热的，但是是直插的体积较大，而且考虑到我们后续板子装在外壳里，所以选了散热和美观平衡一些的TO252（型号是SUD50N06-09L-TP），没用开源工程的PQFN封装。但现在散热还是很依赖pcb设计，而且要确保一级buck使mos压降一直控制在2~4V
- PCB
  ```
  MOS Drain 大焊盘向外铺大铜皮
  上下层都铺铜
  用很多过孔把热导到底层
  底层不要被 LCD 完全贴住
  外壳对应位置开散热孔
  ```

### DAC芯片
- 初始化程序
  ```
  DAC8562_Write24(0x380001);   // 开启内部 2.5V reference，gain 默认变成 2 （2.5V*2=5.0V）
  DAC8562_WriteB(0);           // DAC_CH2 = 0V，电流目标清零
  DAC8562_WriteA(initial_code); // DAC_CH1 给 Buck 初始控制值
  ```

---
# PCB布局与数模地连接（AI提供，仅供参考）


本项目采用两块 PCB：

```text
功率/模拟板：电源、Buck、DAC、模拟开关、OPA、MOS、Rs、采样电路
控制/交互板：STM32、编码器、按键、调试串口、串口屏接口
```

布局重点是：

```text
恒流模拟闭环不跨板
功率回路短而粗
模拟采样参考干净
板间接口只传低速数字信号、低压 ADC 信号和电源
```

---

### 1. 功率/模拟板布局原则

功率/模拟板上放置：

```text
35V 输入与保护
固定 5V Buck
3.3V LDO
可调 Buck 5~30V
DAC8562
TS5A3159A 模拟开关
OPA197
MOS 管
Rs = 2Ω 采样电阻
VOUT+ / VOUT- 采样分压
负载接口
H3 板间接口
```

功率主回路为：

```text
VOUT+ → 负载 → MOS → Rs → GND
```

这条路径要优先布：

```text
走粗线 / 大铜皮
路径尽量短
不要从 DAC、OPA、采样分压下面绕过去
MOS Drain 大焊盘尽量铺铜散热
Rs 到 GND 的功率回路要短而粗
```

---

### 2. GND 与 AGND

功率/模拟板保留两类地：

```text
GND：功率地 / 系统地
AGND：模拟采样参考地
```

#### GND 范围

以下部分接 GND：

```text
35V 输入负端
输入保护
固定 5V Buck
可调 Buck
MOS / Rs 主功率回流
5V / 3.3V 电源部分
H3 普通 GND
```

#### AGND 范围

以下部分接 AGND：

```text
OPA197
DAC8562
TS5A3159A
DAC 输出滤波
OPA 输入相关滤波
VOUT+ / VOUT- 分压下端
Vsense 采样滤波
```

AGND 区域尽量集中在：

```text
DAC / 模拟开关 / OPA / Rs / 采样分压附近
```

不要让下面这些电流流过 AGND：

```text
Buck 开关电流
输入电容充放电电流
MOS 主功率电流
串口屏电流
控制板数字电流
```

---

### 3. GND 与 AGND 的单点连接

GND 和 AGND 不能完全断开，必须单点连接。

连接点放在：

```text
Rs 下端附近 / AGND_STAR
```

用：

```text
GND ── 0Ω / NetTie ── AGND
```

不要在输入模块、Buck 模块、板间接口、串口屏接口附近重复连接 GND 和 AGND。

---

### 4. Rs 的 Kelvin 采样

Rs 既是功率回路的一部分，也是电流采样基准。

主电流路径：

```text
MOS Source → Rs → GND
```

采样路径单独引出：

```text
Rs 上端 Kelvin 点 → OPA197 IN-
Rs 上端 Kelvin 点 → ADC_ISENSE 滤波支路

Rs 下端 Kelvin 点 → AGND_STAR（0Ω电阻处）
```

注意：

```text
大电流从 Rs 功率焊盘走
采样信号从 Rs 焊盘内侧单独拉细线
不要从远处 GND 铜皮取采样参考
```

---

### 5. Buck 与模拟区隔离

两个 Buck 都是主要噪声源。Buck 区域包括：

```text
LMR16030
SW 节点
续流二极管
功率电感
输入电容
输出电容
高 di/dt 回路
```

布局要求：

```text
Buck 区域放在功率/模拟板一侧
OPA / DAC / 采样分压放在另一侧
SW 节点面积尽量小
SW 节点下面不要走 ADC / DAC / OPA 输入线
功率电感远离 OPA、DAC、采样分压
Buck 输入电容紧贴 Buck VIN 和 GND
续流二极管紧贴 SW 和 GND
```

尤其要避开：

```text
ADC_ISENSE
ADC_VOUT+
ADC_VOUT-
DAC_CH1
DAC_CH2
OPA197 IN+
OPA197 IN-
OPA197 OUT / MOS Gate
```

---

### 6. VOUT+ / VOUT- 采样布局

VOUT+ / VOUT- 采样用于：

```text
负载电压显示
Buck 慢环调节
短路 / 开路判断
```

采样点应从真实负载端附近取：

```text
VOUT+：从负载接口 VOUT+ 附近单独引出
VOUT-：从负载接口 VOUT- / MOS Drain 附近单独引出
```

采样分压和滤波放在功率/模拟板上。

注意你们原文里这句需要改：

```text
R25 和 C27 尽量靠近 MCU ADC 引脚
```

拆板后应改成：

```text
功率/模拟板上的 VOUT+ / VOUT- 分压与第一层 RC 滤波放在采样区域内，尽量靠近 H3 前端；
控制板上的 C1/C2/C3 是跨板后 ADC 端的可选小电容，放在 STM32 ADC 引脚附近，默认可不焊。
```

---

### 7. 板间接口相关内容

H3 的详细引脚定义、SPI 旁边配 GND、ADC_REF_GND 如何陪 ADC 信号跨板走，这些放在《接口走线.md》里，不在方案说明里重复。

方案说明里只保留原则：

```text
H3 尽量放在功率/模拟板边缘
SPI 组靠 GND 区走，不穿过 AGND 核心区
ADC 组靠采样区走，远离 Buck 电感、SW 节点和 MOS Gate
ADC_REF_GND 只作为 ADC 采样参考回流，不给屏幕、LED、按键走回流
```

接口走线文档已经说明 ADC_REF_GND 是陪 ADC 信号跨板走的采样参考回流线，并且在控制板 ADC 附近接回 GND。

---

### 8. 控制/交互板布局原则

控制板只用 GND，不大面积分 AGND。

控制板上放：

```text
STM32 核心板
编码器
OUTPUT / STOP 按键
ALARM CLEAR 按键
备用按键
调试串口
串口屏 XH4P 接口
H3 板间接口
```

布局建议：

```text
H3 靠近 STM32 SPI / ADC 引脚一侧
ADC_ISENSE / ADC_VOUT+ / ADC_VOUT- 从 H3 出来后尽量短地到 STM32 ADC
ADC_REF_GND 在 STM32 ADC 附近通过 0Ω / NetTie 接 GND
串口屏 5V / GND 走线要比普通信号线粗
串口屏回流不要穿过 ADC_REF_GND 区域
按键、编码器远离 ADC 线
```

---

### 9. 散热与机械

功率板上发热较明显的器件：

```text
MOS
Rs
可调 Buck 电感
固定 5V Buck 电感
LDO
输入保护器件
```

布局建议：

```text
MOS、Rs、电感尽量靠板边
MOS Drain 铺大铜皮并多打过孔
底层不要被 LCD 完全贴住
若后续三层叠放，功率板和控制板之间留足高度
电感、端子、电解电容区域上方留机械避让
外壳对应 MOS / 电感 / Rs 区域预留散热孔
```

---

### 10. 测试点布局

测试点按两类处理。

#### 跨板信号测试点

这些信号本来就要到 H3，可以把测试点放在 H3 附近：

```text
+5V
3V3
GND
BUCK_EN
SW_IN
SPI_SCLK
SPI_DIN
SPI_SYNC
ADC_ISENSE
ADC_VOUT+
ADC_VOUT-
ADC_REF_GND
```

#### 本地模拟 / 功率测试点

这些点不要为了放到板边而拉很长线，就近放小焊盘：

```text
DAC_CH1
DAC_CH2
V_SENSE_RAW
V_GATE
VOUT+
VOUT-
AGND
FB1
```

Buck 的 SW 节点如果要测，只在本地放极小测试点，不要拉长。

---

### 11. PCB 推荐布线顺序

功率/模拟板：

```text
1. 先布输入保护、两个 Buck 和电源主回路
2. 布 VOUT+ → 负载 → MOS → Rs → GND 功率路径
3. 布 Rs Kelvin 采样
4. 布 OPA / DAC / 模拟开关
5. 布 VOUT+ / VOUT- 采样分压
6. 布 H3 板间接口
7. 最后处理测试点和铺铜
```

控制/交互板：

```text
1. 先放 H3 和 STM32 核心板
2. 布 SPI、BUCK_EN、SW_IN
3. 布 ADC_ISENSE / ADC_VOUT+ / ADC_VOUT-
4. 布串口屏接口
5. 布按键、编码器、调试串口
6. 最后铺 GND
```

---

# 调试流程

## 调试原则
```text
本版为调试板，优先采用单通道实验室电源从整机输入供电。
初期不使用高输入电压，不直接测试满量程 500mA。
先用 9~12V 输入、低目标电流、低功率负载验证电源、DAC、模拟开关、恒流环、ADC、保护逻辑。
所有功能跑通后，再逐步提高输入电压到 20V、30V、35V，并逐步扩大负载和电流范围。
```
## 调试顺序
```text
先电源；
再 MCU / DAC；
再模拟开关；
再可调 Buck 手动调压；
再低电流恒流；
再 ADC 校准；
再保护逻辑；
最后自动 Buck 和满量程测试。
```

### 0. 上电前检查

第一次上电前，两块板先不要叠起来，建议用短排线连接，方便万用表和示波器测。

检查：

```text
1. 功率/模拟板输入正负极无短路。
2. 5V、3V3 对 GND 无短路。
3. AGND 与 GND 只在 0Ω / NetTie 处单点相连。
4. H3 两块板的 pin1 方向一致。
5. BUCK_EN 默认下拉，可调 Buck 默认关闭。
6. SW_IN 默认下拉，模拟开关默认接 AGND。
7. DAC_CH2 默认 0V。
8. 负载先不接，串口屏先不接。
```

上电默认状态应该是：

```text
BUCK_EN = 0
SW_IN = 0
DAC_CH2 = 0
可调 Buck 不输出
MOS 关断
输出电流 = 0
```

---

### 1. 低压单通道供电测试

先用实验室电源接整机输入，只用一个通道。

初始建议：

```text
VIN = 9V ~ 12V
限流 = 100mA ~ 300mA
负载不接
串口屏不接
```

先测功率/模拟板：

```text
TP_VIN / VBUS：输入电压正常
TP_5V：约 5V
TP_3V3：约 3.3V
TP_VOUT+：应接近 0V，或可调 Buck 未启动
BUCK_EN：低电平
SW_IN：低电平
DAC_CH2：0V
```

如果 5V 或 3V3 不正常，不要继续接控制板和屏幕。

注意：你们功率板上有“输入不要超过 40V / DC JACK DNP / 30V ONLY”的提示，高压阶段要确认实际使用的是能承受对应电压的输入接口。

---

### 2. 接控制板，但仍不接负载

连接 H3，给控制板供电。此时仍然保持：

```text
VIN = 9V ~ 12V
限流 = 300mA 左右
串口屏先不接
负载不接
```

测试：

```text
STM32 能下载程序
调试串口正常
3V3 没有明显下跌
H3 两端 +5V、3V3、GND 正常
BUCK_EN 软件初始化后仍保持 0
SW_IN 软件初始化后仍保持 0
```

然后测试 DAC：

```text
DAC_CH2 设 0mA   → 约 0V
DAC_CH2 设 50mA  → 约 0.1V
DAC_CH2 设 100mA → 约 0.2V
```

因为：
```
V_DAC_CH2=I_set * R_s
R_s=2Ω
```
---

### 3. 测模拟开关

保持负载不接，可调 Buck 仍关闭。

测模拟开关 COM / OPA IN+：

```text
SW_IN = 0：
COM ≈ 0V
OPA IN+ ≈ 0V

SW_IN = 1：
COM ≈ DAC_CH2
OPA IN+ ≈ DAC_CH2
```

如果 SW_IN=0 时 OPA IN+ 不是 0V，要先查模拟开关接法和下拉。

---

### 4. 可调 Buck 低压手动测试

这一阶段还不要自动 Buck。

先让软件手动设置 `DAC_CH1`，再打开 `BUCK_EN`。

顺序：

```text
1. BUCK_EN = 0
2. DAC_CH1 设到较高值，让 Buck 目标低输出
3. 打开 BUCK_EN
4. 测 VOUT+
```

你们现在可调 Buck 近似关系是：

[
V_{OUT}\approx30.98-10V_{DAC_CH1}
]

但在低输入电压下，Buck 不可能输出高于输入，所以低压阶段只测试低输出：

```text
VIN = 12V 时：
只测试 VOUT+ ≈ 5V ~ 8V
不要命令 VOUT+ 到 20V / 30V
```

建议先测：

```text
VOUT+ ≈ 5V
VOUT+ ≈ 6V
VOUT+ ≈ 8V
```

确认 DAC_CH1 越高，VOUT+ 越低。

---

### 5. 恒流环低风险测试

现在开始接负载，但严格限制电流和负载组合。

低压阶段建议：

```text
VIN = 12V
Iset ≤ 100mA
负载：10Ω、25Ω、50Ω
```

测试顺序：

```text
1. RL = 10Ω，Iset = 20mA
2. RL = 10Ω，Iset = 50mA
3. RL = 10Ω，Iset = 100mA

4. RL = 25Ω，Iset = 50mA
5. RL = 25Ω，Iset = 100mA

6. RL = 50Ω，Iset = 50mA
7. RL = 50Ω，Iset = 100mA
```

每次观察：

```text
Vsense = Iset × 2Ω
Vgate 是否稳定
VOUT+ 是否合理
VOUT- 是否合理
MOS 是否发热
```

示波器建议：

```text
CH1：V_SENSE_RAW / Vsense
CH2：Vgate
```

低压阶段不要测 500mA，也不要长时间短路。

---

### 6. 低压保护功能测试

先用低阈值或低电流测试保护逻辑，不要一开始就真实 550mA。

#### 短路保护

```text
VIN = 12V
Iset = 50mA 或 100mA
短接负载端
```

应看到：

```text
电流被限制在设定值附近
LCD/串口输出显示短路限流
Buck 不应维持高 VOUT+
MOS 不应明显发热
```

#### 开路保护

```text
VIN = 12V
Iset = 50mA
断开负载
```

应看到：

```text
Iactual ≈ 0
系统识别开路
SW_IN = 0
DAC_CH2 = 0
BUCK_EN = 0
报警锁存
```

#### 过流保护

低压阶段建议先用软件临时降低阈值验证逻辑，例如：

```text
临时阈值：120mA 或 150mA
```

确认逻辑无误后，后期再恢复：

[
I>550mA
]

对应：

[
V_{sense}>1.1V
]

---

### 7. 逐步提高输入电压

所有低压功能跑通后，再提高输入电压。

建议分三档：

| 阶段 |   输入电压 | 最大建议电流 | 负载范围        | 目的         |
| -- | -----: | -----: | ----------- | ---------- |
| A  |  9~12V |  100mA | 10Ω/25Ω/50Ω | 基础功能、低风险联调 |
| B  | 18~20V |  250mA | 10Ω/25Ω/50Ω | 中功率、保护逻辑   |
| C  | 30~35V |  500mA | 10Ω/25Ω/50Ω | 满量程测试      |

判断某组负载和电流是否能测，用：

[
V_+ \approx I R_L + I R_s + V_{MOS余量}
]

其中：

```text
Rs = 2Ω
VMOS 余量取 2V~4V
```

例如 50Ω、500mA：

[
V_+ \approx 0.5\times50 + 0.5\times2 + 2\sim4
]

[
V_+ \approx 28V\sim30V
]

所以这组必须等高输入电压阶段再测。

---

### 8. 自动 Buck 调节测试

等手动 Buck、恒流、ADC 都正常以后，再开自动 Buck。

软件目标：

[
V_{MOS}=V_{OUT-}-V_{sense}
]

目标范围：

```text
VMOS ≈ 2V~4V
```

简单策略：

```text
VMOS < 1.5V：
    提高 VOUT+

VMOS > 5V：
    降低 VOUT+

1.5V~5V：
    保持
```

调节不要太快：

```text
每 50ms~200ms 调一次
每次 DAC_CH1 小步变化
```

---
# 第二版改进
1. 换mos：
```
ai：
严格说，它在你们正常控制下未必会直接承受 40V。因为可调 Buck 最高只有 30V 左右，低边恒流 MOS 的 Vds 多数时候来自 Buck 输出，而不是 40V 输入。

但问题是：你们这个 MOS 是线性调整管，不是单纯开关管。它更应该看：

Vds 裕量
SOA 安全工作区
散热能力
Vgs 逻辑电平驱动能力

60V 能用，但不够舒服。最终 40V 输入下，我建议换成：

Vds ≥ 80V，最好 100V
逻辑电平 MOS
封装优先 TO-220 / TO-252 / DPAK
重点看 SOA，而不是只看 Rds_on

推荐方向：

IRL540N：100V，TO-220，方便散热
100V 逻辑电平 DPAK / TO-252 MOS，优先选有 SOA 曲线的

调试板可以先不换
```